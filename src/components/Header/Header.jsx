import './Header.css';

const Header = () => {
  return (
    <header>
      <div>
        <h2>DEMO Streaming</h2>
        <a>Log in</a>
        <button>Start your free trial</button>
      </div>
    </header>
  );
};

export default Header;
